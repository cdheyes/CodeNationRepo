let password = "";
password += "123456789"; // password must be 8 chars long
let length = password.length;

if (length < 8) {
    console.log("That password is to short")} // if less the 8
else {console.log(`The password is ${password}`) // else all is well
}
